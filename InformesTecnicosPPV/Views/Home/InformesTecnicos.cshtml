@{
    ViewBag.Title = "Informes Tecnicos";
}
<h2>@ViewBag.Title</h2>

<div class="row" style="text-align:right;padding: 0px 15px;">
    <a href="~/Home/CargarInforme" class="btn btn-info"><span class="glyphicon glyphicon-plus"></span> Cargar Informe</a>
</div>
<br />
<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Fecha</th>
                    <th>Vencimiento</th>
                    <th>Cliente</th>
                    <th>Informe</th>
                    <th>Tipo de Informe</th>
                    <th>Descargar</th>
                </tr>
            </thead>
            @*@foreach (var oElemento in Model)
            {
                <tr>
                    <td>@oElemento.RazonSocial</td>
                    <td>@oElemento.NombreFantasia</td>
                    <td>@oElemento.Direccion</td>
                    <td>@oElemento.Cuit</td>
                    <td>@oElemento.Telefono</td>
                    <td>@oElemento.Email</td>
                    <td>@oElemento.Responsable</td>
                </tr>

            }*@
        </table>
    </div>

</div>